# Bichhu Gang — Vercel Ready

Clean static edition for direct deployment from Termux.

Deploy:
```bash
cd ~/bichhu-gang-v5
npm install -g vercel
vercel login
vercel --prod
```

No external backend service is configured in this edition.
